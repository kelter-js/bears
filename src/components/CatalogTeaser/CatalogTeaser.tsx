import Typography from "../common/Typography";

import { Container } from "./CatalogTeaser.styled";

export const CatalogTeaser = () => (
  <Container>
    <Typography variant="h1" className="teaser">
      Каталог товаров
    </Typography>
  </Container>
);
